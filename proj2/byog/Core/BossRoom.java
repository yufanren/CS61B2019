package byog.Core;

// A room that is larger than average.
public class BossRoom extends Room {

    public BossRoom(MapGen map, Position corner, int width, int height) {
        super(map, corner, width, height);
    }
}
